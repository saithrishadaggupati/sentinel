apiVersion: apps/v1
kind: Deployment
metadata:
  name: sentinel-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sentinel-redis
  template:
    metadata:
      labels:
        app: sentinel-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "256Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: sentinel-redis
spec:
  selector:
    app: sentinel-redis
  ports:
    - port: 6379
      targetPort: 6379
